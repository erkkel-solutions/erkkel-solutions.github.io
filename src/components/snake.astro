---

---

<div id="snake-ground"></div>

<script>
  import moveSnake from '@utils/snake';

  // TODO: andle resize of the window
  document.addEventListener('DOMContentLoaded', function () {
    moveSnake();
  });
</script>

<style is:global>
  .line {
    position: absolute;
    height: 2px;
    width: 2px;
    z-index: 90;
  }

  .hline {
    background: linear-gradient(90deg, transparent calc(100% / 3), orange 0 calc(2 * 100% / 3), transparent 0);
    background-size: 400% 400%;
  }

  .vline {
    background: linear-gradient(0deg, transparent 33%, orange 0 calc(2 * 100% / 3), transparent 0);
    background-size: 400% 400%;
  }
</style>
